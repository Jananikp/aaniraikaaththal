# Terraform
.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
# Ignore files with .keys or .key extension anywhere
**/*.keys
**/*.key

# Ignore files containing "keys" or "key" in the filename anywhere
# Matches: mykey.txt, private-key, config/keys.json, another_key_file
**/*key*